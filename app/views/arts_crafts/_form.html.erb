<%= form_for(@arts_craft) do |f| %>
  <% if @arts_craft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arts_craft.errors.count, "error") %> prohibited this arts_craft from being saved:</h2>

      <ul>
      <% @arts_craft.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.number_field :parent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
