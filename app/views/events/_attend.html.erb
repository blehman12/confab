<%= form_for(@event.attendances.build(attendee_id: current_user.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :attendee_id %></div>
  <%= f.submit "Attend", class: "btn btn-large btn-primary" %>
<% end %>
