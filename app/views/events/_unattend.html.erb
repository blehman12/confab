<%= form_for(@event.attendances.find_by(attendee_id: current_user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unattend", class: "btn btn-large" %>
<% end %>