<script type="text/javascript">
function HideContent(d) {
document.getElementById(d).style.display = "none";
}
function ShowContent(d) {
document.getElementById(d).style.display = "block";
}
function ReverseDisplay(d) {
if(document.getElementById(d).style.display == "none") { document.getElementById(d).style.display = "block"; }
else { document.getElementById(d).style.display = "none"; }
}
function HideAllShowOne(d) {
var IDvaluesOfEachDiv = "wantform skillform";

//-------------------------------------------------------------
IDvaluesOfEachDiv = IDvaluesOfEachDiv.replace(/[,\s"']/g," ");
IDvaluesOfEachDiv = IDvaluesOfEachDiv.replace(/^\s*/,"");
IDvaluesOfEachDiv = IDvaluesOfEachDiv.replace(/\s*$/,"");
IDvaluesOfEachDiv = IDvaluesOfEachDiv.replace(/  +/g," ");
var IDlist = IDvaluesOfEachDiv.split(" ");
for(var i=0; i<IDlist.length; i++) { HideContent(IDlist[i]); }
ShowContent(d);
}</script>
<h1>Confab8 Demo App</h1>
<p>This is the home page. It could either be primarily dedicated to getting users to sign in/up or it could allow for browsing of various conferences currently on Confab.</p>

<% if signed_in? %>
<% @user ||= current_user %>
  <section>
    <%= render 'shared/stats' %>
  </section>
  <aside class="span4">
    <a href="javascript:ReverseDisplay('wantform')">Add a New Interest</a> |
    <a href="javascript:ReverseDisplay('skillform')">Add a New Specialty</a>
    <section id="wantform" style="display:none">
      <%= render 'shared/want_form' %>
    </section>
    <section id="skillform" style="display:none">
      <%= render 'shared/skill_form' %>
    </section>
  </aside>

<br>

  <aside class="span4">
    <section>
      <%= render 'shared/wantlist' %>
    </section>
    <br>
    <section>
      <%= render 'shared/skilllist' %>
    </section>
  </aside>
<% else %>
  <p>Please <%= link_to 'sign in', signin_path %> to get started.</p>
<% end %>